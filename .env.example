# ---------------------------------------------------------------------------
# Loja Vênus — variáveis de ambiente
# Copie para `.env` (local) e para as Environment Variables da Vercel (deploy).
# NUNCA versione o arquivo `.env` com valores reais.
# ---------------------------------------------------------------------------

# --- Banco de dados (PostgreSQL no Railway) ---------------------------------
# Railway > seu projeto > serviço Postgres > aba "Connect" > "Postgres Connection URL".
# Para desenvolvimento local use a URL pública (proxy); em produção prefira a
# variável interna (DATABASE_URL) que a Railway/Vercel expõem.
DATABASE_URL="postgresql://user:password@host:port/railway"

# --- Auth.js (NextAuth v5) ------------------------------------------------
# Gere com: npx auth secret   (ou: openssl rand -base64 33)
AUTH_SECRET=""
# Em produção defina a URL pública do app (ex.: https://lojavenus.vercel.app).
# Em local pode deixar em branco (default http://localhost:3000).
AUTH_URL="http://localhost:3000"
# Confia no host quando atrás de proxy (Vercel/Railway).
AUTH_TRUST_HOST="true"

# --- Usuário admin inicial (usado pelo seed: `npm run db:seed`) -----------
ADMIN_EMAIL="admin@lojavenus.com.br"
ADMIN_PASSWORD="troque-esta-senha"
ADMIN_NAME="Administrador"

# --- Loja (migrado de src/config/store.ts) -------------------------------
NEXT_PUBLIC_STORE_NAME="Loja Vênus"
NEXT_PUBLIC_STORE_WHATSAPP_NUMBER="5511999999999"
NEXT_PUBLIC_STORE_TAGLINE="Perfumes, cosméticos e presentes que iluminam você"

# --- Cloudinary (Fase 1 — imagens de produto) ---------------------------
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=""
# API Key pública — exposta ao navegador; exigida pelo CldUploadWidget para
# uploads assinados. Use o MESMO valor de CLOUDINARY_API_KEY (a key não é
# secreta; apenas CLOUDINARY_API_SECRET é sigilosa e nunca vai ao cliente).
NEXT_PUBLIC_CLOUDINARY_API_KEY=""
CLOUDINARY_API_KEY=""
CLOUDINARY_API_SECRET=""

# --- Integrações de fases futuras (placeholders) -----------------------
# Pagamento (Fase 4 — Mercado Pago)
MERCADOPAGO_ACCESS_TOKEN=""
MERCADOPAGO_WEBHOOK_SECRET=""
# Frete (Fase 5 — Melhor Envio / Frenet)
MELHORENVIO_TOKEN=""
# WhatsApp Cloud API (Fase 7)
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_VERIFY_TOKEN=""
# NF-e (Fase 8 — Focus NF-e / NFe.io / PlugNotas)
NFE_PROVIDER_TOKEN=""
